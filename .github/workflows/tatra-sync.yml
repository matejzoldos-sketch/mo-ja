# Tatra banka AIS → Supabase. Token: refresh_token (odporúčané) alebo client_credentials.
# Secrets: SUPABASE_*, TATRA_CLIENT_ID, TATRA_CLIENT_SECRET; voliteľne TATRA_REFRESH_TOKEN.

name: Tatra banka sync

on:
  schedule:
    # 00:30 UTC — pol hodiny po Shopify sync (00:00 UTC).
    - cron: "30 0 * * *"
  workflow_dispatch:
    inputs:
      days:
        description: "Počet dní spätne (booking)"
        required: true
        default: "45"
      tatra_env:
        description: "Tatra API (OAuth + Premium API koreň)"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - sandbox

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
  TATRA_CLIENT_ID: ${{ secrets.TATRA_CLIENT_ID }}
  TATRA_CLIENT_SECRET: ${{ secrets.TATRA_CLIENT_SECRET }}
  TATRA_REFRESH_TOKEN: ${{ secrets.TATRA_REFRESH_TOKEN }}

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TATRA_ENV: ${{ github.event_name == 'workflow_dispatch' && inputs.tatra_env || 'production' }}
      TATRA_TX_DATE_FROM_MIN: "2025-11-20"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - run: pip install -r requirements.txt

      - name: Run sync_tatra
        run: python etl/sync_tatra.py --days "${{ github.event_name == 'workflow_dispatch' && inputs.days || '45' }}"
